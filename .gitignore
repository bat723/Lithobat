# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/

# OS junk
.DS_Store
Thumbs.db

# Local-only Obsidian vault — never publish (see docs/Publishing and Privacy.md)
docs/

# Generated ML artifacts (regenerable via scripts/generate_data.py / litho_sim.ml)
dataset/
inference_results*/
checkpoints/

# Local reference material (copyrighted, keep out of history)
/*.pdf

# Vault backup tarballs
docs-backup-*.tar.gz

# Device preset cache (rebuildable: File > Load device, or scripts/show_device.py)
presets/

# Obsidian config — the vault lives at docs/ (itself ignored); a stray root
# vault once put .obsidian/ here and it must never be committed.
.obsidian/

# Figures every demo rewrites. Only the ones the README shows stay tracked.
results/*
!results/opc_demo.png
!results/gaa_steps.png
!results/qt_app_window.png
!results/resist_profile_3d.png
!results/vector_effect.png
!results/multipatterning_demo.png
!results/process_window.png
!results/stochastic_demo.png

# The autoencoder study's talk material and result previews — regenerable
# from the ml extra, and not part of the engine.
slides/
/dataset_preview.png
/mse_distribution.png
