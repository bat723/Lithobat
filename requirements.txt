# The package's dependencies live in pyproject.toml; this file exists for
# `pip install -r requirements.txt` habits and installs the same thing:
# the engine, the desktop app, and the test tooling.
#
#   pip install -e ".[app,dev]"      # equivalent, and the recommended form
#
# Optional extras (see pyproject.toml):  viz (plotly, scikit-image)
#                                        ml  (torch, torchvision, Pillow, pyyaml)
-e .[app,dev]
