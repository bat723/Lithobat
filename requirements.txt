# Core simulation stack
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
tqdm>=4.65

# Machine learning (optional — install with: pip install -r requirements.txt -r requirements-ml.txt)
# torch>=2.0
# torchvision>=0.15
# scikit-image>=0.21
# Pillow>=10.0
# pyyaml>=6.0

# Development / testing
pytest>=7.4
pytest-cov>=4.1

# UI and 3-D visualisation
streamlit>=1.33
plotly>=6.0

# Optional: marching-cubes meshes in viz3d (freestanding spacers, undercut).
# viz3d falls back to height-field surfaces without it.
# scikit-image>=0.21
